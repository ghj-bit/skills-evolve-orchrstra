# 附件

原附件中的图片与 HTML 表格已转写为结构化数据和文字说明，不再依赖 `images/` 目录。

请直接使用规范化版本：

- [`normalized/附件_规范化.md`](./normalized/附件_规范化.md)

规范化后保留的核心文件包括：

- 各关说明文件 `normalized/level_*_info.md`
- 已知天气序列 `normalized/level_*_weather.csv`

这些文件保留了解题所需的最小信息集合，删除原图片后仍可独立使用。
